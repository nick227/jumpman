# Audio Drop Folder

Binary `.ogg` and `.mp3` mock cues live outside of source control. Add your own effect pairs
here following the names defined in `src/config/audioMap.js` (e.g. `coin.ogg` / `coin.mp3`).
The development server serves this folder directly from `public/audio/`.
